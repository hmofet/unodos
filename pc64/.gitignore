# build artifacts + harness scratch
build/
# Intel/Realtek WiFi firmware blobs (non-redistributable; fetched locally)
fw-blobs/
__pycache__/
*.pyc
*.o
*.pcap
*.log
tls_test/server.key
tls_test/server.crt

# MicroPython port build artifacts + developer-supplied Doom WADs
build/upyhost/
# the WAD data itself is never committed; the README that explains how to get
# it is kept.
wads/*.wad
wads/*.WAD
wads/*.zip
