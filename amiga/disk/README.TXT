UnoDOS/68K on Amiga
-------------------
This file lives on the boot
ROM-disk: files are baked into
the kernel image at build time
and editable in RAM.

Open me in Notepad, type, and
press F1 to save (RAM only,
until the MFM/FAT12 driver
lands).
