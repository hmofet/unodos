/* csslib/compat/sys/types.h - libwapcaplet's one POSIX-header include.
 * Only the types the stack actually touches. */
#ifndef CSSLIB_COMPAT_SYS_TYPES_H
#define CSSLIB_COMPAT_SYS_TYPES_H

#include <stddef.h>
#include <stdint.h>

typedef long long ssize_t;

#endif /* CSSLIB_COMPAT_SYS_TYPES_H */
